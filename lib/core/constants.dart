import 'package:flutter/material.dart';

/* COLORS */
const defaultAccentColor = Color(0xFFED5992);
const defaultBackgroundColor = Color(0xFFFFFFFF);
const defaultTitleColor = Color(0xFF2F1767);
const defaultSubtitleColor = Color.fromRGBO(140, 140, 140, 1);
const grayLight = Color.fromRGBO(200, 200, 200, 1);
const invisible = Color.fromARGB(0, 255, 255, 255);
