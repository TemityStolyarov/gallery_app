import 'package:flutter/material.dart';

/* COLORS */
const DEFAULT_ACCENT_COLOR = Color(0xFFED5992);
const DEFAULT_BACKGROUND_COLOR = Color(0xFFFFFFFF);
const DEFAULT_SUBTITLE_COLOR = Color.fromRGBO(140, 140, 140, 1);
const GRAY_LIGHT = Color.fromRGBO(200, 200, 200, 1);
const INVISIBLE = Color.fromARGB(0, 255, 255, 255);
